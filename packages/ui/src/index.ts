/**
 * Flow UI — Design System da PeopleFlow (doc 13).
 *
 * Apps consomem SOMENTE esta API — nunca Radix/shadcn diretamente (ADR-012).
 * Catálogo v1 mínimo; cresce com Storybook + testes axe na Fase 1 completa.
 */
export { cn } from "./cn";
export { Button, type ButtonProps } from "./button";
export { Input, Label } from "./input";
export { Card, CardHeader, CardTitle, CardContent, StatCard } from "./card";
export { Badge, type BadgeProps } from "./badge";
